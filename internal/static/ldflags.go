package static

var (
	// AppVersion describes the version of the build
	AppVersion = "UNSET"
	// AppCommit describes the commit of the build
	AppCommit = "UNSET"
	// Release describes if te build is a release
	// build or not
	Release = "FALSE"
)
